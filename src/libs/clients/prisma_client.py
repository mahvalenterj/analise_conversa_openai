# prisma_client.py

from prisma import Prisma

# Inicializando o cliente Prisma
prisma = Prisma()

# Acesso aos tipos definidos pelo Prisma (se necessário)
# Esses tipos seriam representados como modelos em Python
# mas não há equivalente direto para "type" no Python como no TypeScript
# portanto você acessaria os modelos diretamente do prisma

# Exemplo de uso
# analysis = prisma.analysis
# message = prisma.message
# motel = prisma.motel
# session = prisma.session
